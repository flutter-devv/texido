import 'package:texido_app/config/sizes.dart';

class AppSize {
  static double size = AppSizes.defaultSize;
  static double xSmall = size * 0.8;
  static double small = size * 0.9;
  static double medium = size;
  static double large = size * 1.2;
  static double xLarge = size * 1.4;
}
